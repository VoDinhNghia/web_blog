<!DOCTYPE html>
<html lang="en">

<head>
    <title>home</title>
    <%- include("./header") %>
</head>

<% if (ModeBG  == 'dark'){%>

    <body background="/images/dark-mode.jpg">
        <%}else{%>

            <body>
                <%}%>

                    <%- include("./menu") %>
                        <div class="container" style="margin-top:70px">
                            <div class="row">
                                <%- include("./study_left") %>
                                    <div class="col-sm-8" id="top_">
                                        <p style="margin-top: 19px;">
                                            <form method="POST" class="form-inline float-right" id="form_create_topic" action="/create/problem">
                                                <div class="form-group mx-sm-3 mb-2">
                                                    <input type="text" name="id_topic" value="<%= InfoTopic[0].ID%>" style="display: none;">
                                                    <input type="text" class="form-control" name="problem" required>
                                                </div>
                                                <button type="submit" class="btn btn-outline-primary mb-2">Problem</button>
                                            </form>
                                        </p>
                                        <br><br><br>
                                        <p class="text-primary text-center">Your questions in the topic
                                            <%= InfoTopic[0].NameTopic%>
                                        </p>
                                        <% if(listAllproblem.length){
                            for (let i = listAllproblem.length - 1; i >= 0; i--){%>
                                            <div class="card">
                                                <div class="card-body">
                                                    <span>
                                    <h5 class="card-title">
                                        <%= listAllproblem[i].ContentProblem%>
                                    </h5>
                                    <h6 class="card-title" style="font-size: 12px;">
                                        <%= listAllproblem[i].ProblemDate%>
                                    </h6>
                                    </span>
                                                    <a href="/solution/for-problem/<%= listAllproblem[i].ID%>" class="btn btn-outline-primary">Solution</a>
                                                    <a href="/delete/problem/<%= listAllproblem[i].ID%>/<%= InfoTopic[0].ID%>" class="btn btn-outline-danger float-right">Delete</a>
                                                </div>
                                            </div>

                                            <%}} else {%>
                                                <p class="text-center">Not found problem.</p>
                                                <%}%>
                                    </div>
                            </div>
                        </div>

                        <%- include("./footer") %>
            </body>

</html>