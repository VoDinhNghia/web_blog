<!DOCTYPE html>
<html lang="en">

<head>
    <title>form new post</title>
    <%- include("./header") %>
</head>

<% if (ModeBG  == 'dark'){%>

    <body background="/images/dark-mode.jpg">
        <%}else{%>

            <body>
                <%}%>
                    <%- include("./menu") %>
                        <div class="container" style="margin-top:60px">
                            <div class="row">
                                <%- include("./content_left_personel") %>
                                    <div class="col-sm-8">
                                        <form action="/newpost" method="POST" id="form_new_post" enctype="multipart/form-data">
                                            <h5 class="text-primary text-muted">Privacy</h5>
                                            <select id="privacy" class="custom-select m-b-15" name="privacy">
                                <option selected>privacy</option>
                                <option value="publish">public</option>
                                <option value="private">private</option>
                            </select>
                                            <br>
                                            <h5 class="text-primary text-muted">Post Title</h5>
                                            <input type="text" name="title_post" class="form-control m-b-15" placeholder="" aria-label="" aria-describedby="basic-addon1" required>
                                            <h5 class="text-primary text-muted">Content</h5>
                                            <textarea class="form-control m-b-15" placeholder="write something" name="content_post" id="exampleFormControlTextarea1" rows="9"></textarea>
                                            <input class="form-control form-control text-primary" name="image_file" id="formFileSm" type="file" />
                                            <br><br>
                                            <button type="submit" class="btn btn-outline-primary">Submit post</button>
                                        </form>

                                    </div>
                            </div>
                        </div>
                        <%- include("./footer") %>
            </body>

</html>